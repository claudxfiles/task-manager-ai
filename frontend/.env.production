# Identity Platform (GCP)
NEXT_PUBLIC_GCP_API_KEY=${NEXT_PUBLIC_GCP_API_KEY}
NEXT_PUBLIC_GCP_AUTH_DOMAIN=${NEXT_PUBLIC_GCP_AUTH_DOMAIN}
NEXT_PUBLIC_GCP_PROJECT_ID=${NEXT_PUBLIC_GCP_PROJECT_ID}

# NextAuth.js
NEXTAUTH_URL=https://souldream.ai
NEXTAUTH_SECRET=${NEXTAUTH_SECRET}

# Google OAuth (Credenciales del servicio SaaS)
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}

# Nota: En producción, estas variables se configuran en el panel de control del proveedor de hosting
# No se requiere que los usuarios finales configuren sus propias credenciales 